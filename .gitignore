# ---------------------------------------------------------------
# Site backups — NEVER commit. The .wpress archive is ~1 GB, far over
# GitHub's 100 MB per-file hard limit. Kept in this folder locally only.
# ---------------------------------------------------------------
*.wpress
*.wpress.*
_backup-extracted/

# ---------------------------------------------------------------
# Database dumps — contain wp_users (emails + password hashes) and
# wp_options (API keys, licence keys). This repo is public.
# ---------------------------------------------------------------
*.sql
*.sql.gz

# ---------------------------------------------------------------
# Secrets & site-local config
# ---------------------------------------------------------------
wp-config.php
.env
.env.*
!.env.example
*.pem
*.key
auth.json

# ---------------------------------------------------------------
# Extracted WordPress tree — this repo is now the theme itself, so a
# wp-content/ folder here would be a local scratch copy, not source.
# ---------------------------------------------------------------
wp-content/

# ---------------------------------------------------------------
# Build & tooling
# ---------------------------------------------------------------
node_modules/
.sass-cache/

# ---------------------------------------------------------------
# Logs
# ---------------------------------------------------------------
*.log
error_log
debug.log

# ---------------------------------------------------------------
# OS / editor cruft
# ---------------------------------------------------------------
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
.idea/
.vscode/
*.swp
